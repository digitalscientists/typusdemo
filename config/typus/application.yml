# Typus Models Configuration File
#
# Use the README file as a reference to customize settings.

Article:
  fields:
    default: title, author
    form: title, body, author
  order_by: 
  relationships: 
  filters: 
  search: title
  application: typus_test
  
Location:
  fields:
    default: name, address, image, image_file_name
    form: name, address, description, image, image_file_name
  order_by: 
  relationships: 
  filters: 
  search: name
  application: typus_test

