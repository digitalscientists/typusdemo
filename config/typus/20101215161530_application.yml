# Typus Models Configuration File
#
# Use the README file as a reference to customize settings.

Location:
  fields:
    default: name, address, image
    form: name, address, description, image
  order_by: 
  relationships: 
  filters: 
  search: name
  application: typus_test

